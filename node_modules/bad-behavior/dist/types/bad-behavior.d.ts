export default function BadBehavior<T>(): {
    next: (n: T) => void;
    subscribe: (fn: ISubscriber<T>) => {
        unsubscribe(): void;
    };
};
export interface ISubscriber<T> {
    (value: T): void;
}
