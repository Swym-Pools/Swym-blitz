/// <reference types="node" />
import { Writable } from "stream";
/**
 * Returns a Stage that writes files to the destination path
 */
export declare const createWrite: (destination: string, reporter: Writable, writeStream?: NodeJS.ReadWriteStream, unlinkStream?: import("stream").Transform) => {
    stream: NodeJS.ReadWriteStream;
};
