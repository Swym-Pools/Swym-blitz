import { Generator, GeneratorOptions, SourceRootType } from "../generator";
export interface ModelGeneratorOptions extends GeneratorOptions {
    modelName: string;
    extraArgs: string[];
}
export declare class ModelGenerator extends Generator<ModelGeneratorOptions> {
    static subdirectory: string;
    sourceRoot: SourceRootType;
    unsafe_disableConflictChecker: boolean;
    getTemplateValues(): Promise<void>;
    getTargetDirectory(): string;
    prismaMigratePrompt(): Promise<boolean>;
    write(): Promise<void>;
    postWrite(): Promise<void>;
}
