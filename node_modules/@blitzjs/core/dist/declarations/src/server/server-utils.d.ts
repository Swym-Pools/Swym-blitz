/// <reference types="node" />
import { IncomingMessage } from "http";
import { BlitzApiRequest } from "../types";
export declare function isLocalhost(req: BlitzApiRequest | IncomingMessage): boolean;
export declare type PaginateArgs<QueryResult> = {
    skip?: number;
    take?: number;
    maxTake?: number;
    count: () => Promise<number>;
    query: (args: {
        skip: number;
        take: number;
    }) => Promise<QueryResult>;
};
export declare function paginate<QueryResult>({ skip, take, maxTake, count: countQuery, query, }: PaginateArgs<QueryResult>): Promise<{
    items: QueryResult;
    nextPage: {
        take: number;
        skip: number;
    } | null;
    hasMore: boolean;
    count: number;
}>;
