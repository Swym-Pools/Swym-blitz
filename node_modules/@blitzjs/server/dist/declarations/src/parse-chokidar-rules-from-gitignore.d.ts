export declare function isControlledByUser(file: string): boolean;
export declare function getAllGitIgnores(rootFolder: string): {
    gitIgnore: string;
    prefix: string;
}[];
export declare function chokidarRulesFromGitignore({ gitIgnore, prefix, }: {
    gitIgnore: string;
    prefix: string;
}): {
    includePaths: string[];
    ignoredPaths: string[];
};
export declare function parseChokidarRulesFromGitignore(rootFolder: string): {
    ignoredPaths: string[];
    includePaths: string[];
};
