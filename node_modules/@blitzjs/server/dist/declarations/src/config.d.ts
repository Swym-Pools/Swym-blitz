import { transformFiles } from "@blitzjs/file-pipeline";
declare type Synchronizer = typeof transformFiles;
export declare type ServerEnvironment = "dev" | "prod";
export declare type ServerConfig = {
    rootFolder: string;
    buildFolder?: string;
    routesFolder?: string;
    clean?: boolean;
    isTypeScript?: boolean;
    watch?: boolean;
    transformFiles?: Synchronizer;
    writeManifestFile?: boolean;
    port?: number;
    hostname?: string;
    inspect?: boolean;
    env: ServerEnvironment;
};
declare type NormalizedConfig = ServerConfig & {
    buildFolder: string;
    routesFolder: string;
    clean?: boolean;
    isTypeScript: boolean;
    watch: boolean;
    transformFiles: Synchronizer;
    writeManifestFile: boolean;
    ignore: string[];
    include: string[];
    nextBin: string;
    env: ServerEnvironment;
};
export declare const standardBuildFolderPath = ".blitz/build";
export declare const standardBuildFolderPathRegex: RegExp;
export declare function normalize(config: ServerConfig): Promise<NormalizedConfig>;
export {};
