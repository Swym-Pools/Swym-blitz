import { Stage } from "@blitzjs/file-pipeline";
export declare const resolverFullBuildPathRegex: RegExp;
export declare const resolverBuildFolderReplaceRegex: RegExp;
export declare const resolverPathRegex: RegExp;
export declare function isResolverPath(filePath: string): RegExpExecArray | null;
/**
 * Returns a Stage that manages generating the internal RPC commands and handlers
 */
export declare const createStageRpc: (isTypeScript?: boolean) => Stage;
