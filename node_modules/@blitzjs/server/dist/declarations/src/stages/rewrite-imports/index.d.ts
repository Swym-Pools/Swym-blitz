import { Stage } from "@blitzjs/file-pipeline";
/**
 * Returns a Stage that converts relative files paths to absolute
 */
export declare const createStageRewriteImports: Stage;
export declare const patternImport: RegExp;
export declare function replaceImports(content: string, cwd: string): string;
export declare function rewriteImportOrigin(origin: string, cwd: string): string;
