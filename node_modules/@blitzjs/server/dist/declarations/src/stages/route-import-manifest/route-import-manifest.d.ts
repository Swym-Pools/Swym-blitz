import { Stage } from "@blitzjs/file-pipeline";
import { OverrideTriage } from "@blitzjs/file-pipeline/src/helpers/work-optimizer";
declare type Parameter = {
    name: string;
    optional: boolean;
};
interface Route {
    name: string;
    parameters: Parameter[];
    multipleParameters: Parameter[];
}
export declare function parseDefaultExportName(contents: string): string | null;
export declare function generateManifest(routes: Record<string, Route>): {
    implementation: string;
    declaration: string;
};
export declare function parseParametersFromRoute(path: string): Pick<Route, "parameters" | "multipleParameters">;
export declare const createStageRouteImportManifest: Stage & {
    overrideTriage: OverrideTriage;
};
export {};
