import { ServerEnvironment } from "../config";
declare type StagesConfig = {
    writeManifestFile: boolean;
    isTypeScript: boolean;
    buildFolder: string;
    env: ServerEnvironment;
};
export declare const configureStages: (config: StagesConfig) => Promise<{
    overrideTriage: import("@blitzjs/file-pipeline/src/helpers/work-optimizer").OverrideTriage;
    stages: import("@blitzjs/file-pipeline").Stage[];
}>;
export declare const configureRouteStages: (config: StagesConfig) => import("@blitzjs/file-pipeline").Stage[];
export declare const configureGenerateStages: () => import("@blitzjs/file-pipeline").Stage[];
export {};
